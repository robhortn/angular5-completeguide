export class Ingredient {
    // public name: string;
    // public amount: number;
    // This shortcuts the assignment of the property vars so you don't have to have them declared separately.
    constructor(public name: string, public amount: number) {
        // this.name = name;
        // this.amount = amount;
    }
}